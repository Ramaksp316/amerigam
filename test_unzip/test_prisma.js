const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const res = await prisma.user.findMany({ where: { accountType: 'PERSONAL', OR: [{ name: { contains: 'a', mode: 'insensitive' } }, { username: { contains: 'a', mode: 'insensitive' } }] }, select: { id: true, name: true, username: true, avatarData: true }, take: 10 }); console.log(res); } catch (e) { console.error(e); } } main().catch(console.error).finally(() => prisma.$disconnect());