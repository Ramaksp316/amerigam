const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const comms = await prisma.community.findMany({ where: { name: { contains: 'Family', mode: 'insensitive' } } }); for (const c of comms) { await prisma.communityMember.deleteMany({ where: { communityId: c.id } }); await prisma.communityMessage.deleteMany({ where: { communityId: c.id } }); await prisma.communityPost.deleteMany({ where: { communityId: c.id } }); await prisma.community.delete({ where: { id: c.id } }); console.log('Deleted:', c.name); } } main().catch(console.error).finally(() => prisma.$disconnect());